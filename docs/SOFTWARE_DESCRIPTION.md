# Software Description Document — Tellumen Climate Risk Platform

## 0. Document control

| Field | Value |
|---|---|
| Document owner | Gaurav Sachdeva |
| Status | Living document — update on every material architecture change |
| Version | 1.0 |
| Last updated | 2026-07-04 |
| Package version (pyproject.toml) | 0.1.0 |

**Maintenance rule:** whenever a hazard type, service, data source, schema table, or auth
mechanism is added/changed/removed, update the relevant section below in the same PR/session as
the code change, and add one line to the [Change log](#12-change-log). Do not let this document
drift from the codebase — treat it as part of the deliverable, not an afterthought. Prefer marking
a section "TBD" over writing an unverified claim.

---

## 1. Executive summary

Tellumen is a multi-tenant climate-risk intelligence platform. It ingests satellite and
reanalysis data, scores physical climate hazards (volcanic, seismic, flood, wildfire, heat,
drought, storm, pollution) at H3-cell resolution, and exposes those scores to three customer
verticals — banking (asset exposure), insurance (parametric contracts, pricing), and agriculture
(supply-chain COGS-at-risk) — through an API, a web UI, and regulatory export packages
(CSRD/ECB/EIOPA/Taxonomy).

## 2. Scope

**In scope:** hazard ingestion and scoring, the operational loop (Sense → Score → Project → Act),
multi-tenant auth/RBAC, the three go-to-market verticals, regulatory reporting exports.

**Out of scope for this document:** day-to-day product roadmap detail (see root-level
`PHASE_*.md` / `IMPLEMENTATION_ROADMAP_PHASED.md`), per-hazard scientific methodology (see the
dedicated `docs/*_METHODOLOGY.md` files, linked in §11), brand/visual identity (`docs/BRAND.md`).

## 3. Architecture overview

| Module | Path | Purpose |
|---|---|---|
| API | `api/` | FastAPI REST service — auth (API keys + JWT), RBAC enforcement, scoring/data endpoints, admin console |
| Ingestion | `services/ingestion/` | Satellite/reanalysis adapters (NASA FIRMS, Copernicus Sentinel-1/3, ERA5, GloFAS, EMSC) and pipeline orchestration |
| Scoring | `ml/scoring/` | Physics-based (volcanic, seismic, storm) and ML-based (flood, wildfire, heat, drought, pollution) hazard scorers |
| Features | `ml/features/` | Feature engineering per hazard for the ML scorers |
| Intelligence | `services/intelligence/` | Vertical-specific logic — agriculture yield risk, supply-chain COGS, insurance pricing/loss curves, benchmarking |
| Regulatory | `services/regulatory_monitoring/` | CSRD/ECB/EIOPA/Taxonomy package generation, versioning, audit trail |
| Notifications | `services/notifications/` | Alerts, webhooks, email/SMS dispatch |
| Core | `core/` | DB models, session management, H3 geospatial utilities, config |
| UI | `ui/` | React/Vite frontend — risk map, ops dashboard, parametric contract view, compliance export |

Data flow: adapters (`services/ingestion`) → raw observations → hazard scorers (`ml/scoring`) →
append-only `canonical_scores` → consumed by `api/` for the UI, vertical intelligence modules, and
regulatory export.

## 4. Hazard catalog

| Hazard | Approach | Scorer | Primary data | Backtest anchor |
|---|---|---|---|---|
| Volcanic | Physics (distance-decay + curated zones) | `ml/scoring/volcanic_physics.py` | GVP eruption catalog + `volcanic_hazard_zones` | Fuego 2018, Taal 2020 |
| Seismic | Physics (IPE attenuation) | `ml/scoring/seismic_physics.py` | EMSC WFS, USGS | — |
| Storm | Physics (wind-decay) | `ml/scoring/storm_physics.py` | ERA5 wind, TC track data | — |
| Flood | ML (XGBoost ensemble) | `ml/scoring/engine.py` + `ml/features/flood.py` | Sentinel-1 SAR, ERA5 precip, GloFAS | 2021 Ahr Valley |
| Wildfire | ML | `ml/scoring/engine.py` + `ml/features/wildfire.py` | Sentinel-3 LST, ERA5, NASA FIRMS | 2022 Gironde |
| Heat | ML | `ml/scoring/engine.py` + `ml/features/heat.py` | ERA5, Sentinel-3 LST | 2003 European heatwave |
| Drought | ML | `ml/scoring/engine.py` + `ml/features/drought.py` | ERA5 precip, SPEI, GloFAS | — |
| Pollution | ML (WHO AQG-anchored) | `ml/scoring/pollution_aqi.py` | Copernicus CAMS | — |

Full scientific methodology and known limitations for each hazard live in the corresponding
`docs/*_METHODOLOGY.md`. **Note on FIRMS:** it feeds the wildfire scorer above; for volcanic it is
a supplementary monitoring signal only and is *not* an input to the volcanic score — see
`docs/VOLCANIC_HAZARD_METHODOLOGY.md` §2 for the full caveat.

## 5. Data architecture

Core tables (PostgreSQL + TimescaleDB + PostGIS):

- **Observations/ingestion:** `satellite_observations`, `seismic_events`, `flood_observations`,
  `wildfire_observations`, `heat_observations`
- **Scoring:** `canonical_scores` (append-only golden source: `h3_cell`, `hazard_type`,
  `risk_score`, `shap_factors`, `model_version`, `data_vintage`), `model_registry`
- **Financial/risk:** `parametric_contracts`, `contract_triggers`, `bank_assets`,
  `climate_hazard_exposure`, `damage_assessment`
- **Regulatory:** `regulatory_frameworks`, `regulation_versions`, `regulatory_package`,
  `sc_model_validation`
- **Asset-manager securities book:** `issuers`, `issuer_facilities`, `securities`, `funds`,
  `fund_positions`, `issuer_emissions`, `issuer_transition_scores` — the issuer/footprint/fund
  graph (distinct from the located-asset `portfolio_entities` model), keyed to the same golden
  source via `issuer_facilities.h3_cell → canonical_scores`.
- **Reference-data provenance:** every `issuers`/`securities`/`issuer_facilities`/`issuer_emissions`
  row carries `source` + `data_vintage` (+ `confidence` on facilities); `reference_resolution_log`
  records one row per ISIN resolution attempt (resolved/cached/unmatched/error) — the audit trail
  an SFDR filing cites.
- **Multi-tenancy:** `organizations`, `users`, `roles`, `permissions`, `api_keys`

`canonical_scores` is append-only; scores are never mutated in place, only superseded
(`valid_to`), which is what makes the backtest/audit trail reproducible.

**Reference-data resolution (open-data, no vendor license).** `services/reference/` turns a bare
ISIN — all an asset-manager client supplies — into an auditable issuer→security→footprint graph:
GLEIF (ISIN→LEI→issuer identity, free/keyless) resolves the issuer; the GLEIF headquarters address
is geocoded (Nominatim/OSM) and snapped to H3 res-8 to seed a footprint facility, scored via the
same any-address path (`services.scoring.on_demand`). Sector/NACE (absent from GLEIF) and
multi-facility footprints/emissions are surfaced as coverage gaps, never fabricated.

## 6. Security, multi-tenancy & access control

Tenancy model: `organizations` → `users` → `roles` → `permissions` (many-to-many). API auth uses
hashed API keys (`cp_live_<32hex>`, SHA-256 at rest, shown once at creation); session auth uses
JWT + bcrypt. Roles are org-scoped (admin, analyst, reporter, viewer); permissions are granular
(e.g. `admin.users.manage`, `data.scores.read`, `regulatory.export`). The admin console
(`api/routers/admin.py`) handles user/role/API-key lifecycle, with every mutation written to
`access_audit_log`. Enforcement is centralized in `api/services/rbac.py` and
`api/deps.py::require_permission()`.

## 7. Deployment & infrastructure

- `infra/docker/Dockerfile` — multi-stage build: `api-deps` (FastAPI + XGBoost/LightGBM/MLflow),
  `ingest-deps` (GDAL/HDF5/NetCDF/rasterio); produces an API image (Uvicorn, `:8000`, health
  check) and a worker image (per-hazard-type scoring process).
- `docker-compose.yml` — local dev stack: PostgreSQL 16 + TimescaleDB (`:5433`), MLflow (`:5001`,
  SQLite backend), Adminer (`:8080`).
- Database requires PostGIS and `uuid-ossp` extensions in addition to TimescaleDB.

## 8. External interfaces

REST API (`api/`) is the sole external interface: scoring/data endpoints, admin/user management,
regulatory export. No other externally-facing services currently exist. Data adapters
(`services/ingestion/adapters/`) are the platform's only outbound integrations: NASA FIRMS,
Copernicus Sentinel-1/3, ERA5, GloFAS, EMSC. The reference-data layer (`services/reference/`) adds
two further outbound open-data integrations: GLEIF (LEI/ISIN resolution) and Nominatim/OSM
(headquarters geocoding).

## 9. Non-functional characteristics

TBD — no verified SLAs, throughput, or scaling benchmarks exist yet. Do not populate this section
with estimates; fill in once real numbers (load tests, prod metrics) exist.

## 10. Known limitations (platform-level)

- No centralized `CHANGELOG.md`/`VERSION` file — versioning today is implicit via git history,
  `model_registry.model_version`, and `regulation_versions.version_number`. This document's own
  §12 change log is the first step toward closing that gap for architecture-level changes; a
  code-level CHANGELOG is a separate, not-yet-scheduled task.
- Per-hazard scientific limitations are documented individually in each
  `docs/*_METHODOLOGY.md` — this document intentionally does not restate them.

## 11. Related documents

- `docs/VOCABULARY.md` — canonical hazard/scenario/risk-bucket terms
- `docs/MULTI_SECTOR.md` — banking/insurance/agriculture architecture
- `docs/*_METHODOLOGY.md` — one per hazard type (volcanic, seismic, storm, pollution, supply-chain)
- `docs/BACKTEST_DATA.md` — backtest event dataset status
- `docs/investor_brief.md`, `docs/objection_tracker.md` — external/market-facing material

## 12. Change log

| Date | Version | Change |
|---|---|---|
| 2026-07-31 | 1.50 | **Golden-source registry expanded to the real source breadth + investor functional-architecture diagram.** The freshness registry (`services/data/feeds.py`) under-represented what the engine actually ingests; expanded **6 → 10 feeds**, each backed by a real adapter/module: **flood** (`adapters/glofas.py` + terrain), **fire & thermal** (FIRMS · `sentinel3_slstr.py`), **atmosphere** (`pollution_cams.py` — CAMS), **radar & optical imagery** (`sentinel1_sar.py` · Sentinel-2), **storms & ocean** (IBTrACS · `storm_physics.py` · Copernicus Marine), **geophysical** (`seismic_physics.py` · `volcanic_physics.py` — USGS/GVP/GEM), plus climate reanalysis (ERA5), deforestation (Hansen), and reference (GLEIF; Climate TRACE + GEM merged). Tightened **`invalidates_basis`** semantics: it now means "affects an un-frozen ESRS/EUDR filing" — so **atmosphere** (E2 pollution out of scope) and **geophysical** (not climate-attributable) are `False` even though they feed the risk view. Auto-flows to the Control-Center **Golden-source freshness** panel (now 10 rows). Old-key refresh-log rows cleaned. **Investor functional-architecture diagram** added under `docs/architecture/` (pptxgenjs, two colourways green/gold + blue): the golden-source Earth core, the Sense→Score→Project→Act loop, the honesty-gate valve labelled **"our publish standard"** (r² ≥ 0.40 is *our* constant, not an industry benchmark), and the two-sided value chain (Operate + Comply → Regulator·next); data-in shows the 8 source domains with relative-scale bars + cadence, with "near-real-time" honestly resting on the NRT feeds (FIRMS/GloFAS/Sentinel/cyclone tracks), and seismic/volcanic labelled *geophysical* (natural-catastrophe superset, not r²-calibrated). Registry verified live (10 feeds, correct basis flags). |
| 2026-07-30 | 1.49 | **Regulated side finished — geocoder productionization + golden-source freshness.** The last two GAP(us) items on the entity side, both now built (only external/config remains). **(D) Geocoder** (`services/geocoding/geocoder.py` + `geocode_cache` table): a cache-aware, provider-swappable front door — every resolved query is persisted (keyed on provider + normalized query + limit) so bulk/repeat uploads hit Postgres not the provider; Nominatim results now carry a **real confidence + precision + `low_confidence`** flag (from `importance`/`addresstype`, replacing the flat 0.6), threaded into site/plot creation and the `/geocode` autocomplete, which now **warns on a coarse (city/region/country-level) hit**; `GEOCODER_PROVIDER` config selects the backend (paid/self-host = config, not code). **(E) Golden-source freshness** (`services/data/feeds.py` + `feed_refresh_log` table): a registry of the satellite/agency + reference feeds (Copernicus/ECMWF, NASA/USGS, Hansen GFC, GLEIF, Climate TRACE, GEM), each with a cadence + `invalidates_basis` flag; freshness computed **fresh/due_soon/overdue/untracked**; `GET /v1/admin/data-feeds` + `POST …/{key}/refresh` (audited); a **Golden-source freshness** panel in the Control Center (status dots, "scores" tag on basis-invalidating feeds, Record-refresh). Honest boundary: recording a refresh stamps the log/can trigger a re-score — the data pull itself stays a scheduled/external job. Verified: geocode cache hit on repeat, address-level 0.87 vs coarse hit flagged; feeds untracked→fresh after refresh; panel renders. tsc + web build pass. Runbook §3/§5 → READY; the regulated side is functionally complete. |
| 2026-07-30 | 1.48 | **Point-1 workstream B — EUDR Tier-2 TRACES submission client (prepared-by-default, config-flip to live).** Closes the last piece of "own the reporting last-mile": the direct-to-TRACES path, built honestly since we can't call the real EU system here. `services/intelligence/traces_client.py`: `build_submission()` maps our assembled DDS (operator EORI, HS headings, per-plot GeoJSON geolocations, countries of production, deforestation determinations) to a TRACES-shaped envelope with a deterministic internal reference; `submission_preview()` (`GET /v1/supply/eudr/submission-preview`) shows exactly what would be filed with **no side effects**; `submit_dds()` (`POST /v1/supply/eudr/submit`, audited) runs in **`prepared`** mode by default (assemble → completeness-gate → return the envelope, **files nothing**) and flips to **`live`** (real `urllib` POST) only when `TRACES_MODE=live` + `TRACES_BASE_URL` + `TRACES_API_TOKEN` are set. Honesty rails: a blocked book returns its blockers; live-without-creds returns an explicit `not_configured` (never a fake success); the envelope carries a `_mapping_note` that field names must be aligned to the published EUDR IS schema before live. A "Prepare TRACES submission" button on the Disclosure page beside the Tier-1 manual capture. Verified on Terra Foods (preview ready=true, envelope built with EORI + cocoa HS + GeoJSON, deterministic ref TELLUMEN-…; prepared submit files nothing; live-no-creds → not_configured). tsc + web build pass. **Point 1 is now complete** — runbook §1 → READY, closing note updated; SOFTWARE_DESCRIPTION v1.48. |
| 2026-07-30 | 1.47 | **Point-1 workstream C — assurance evidence pack (auditor-ready ZIP, keyed to a snapshot).** CSRD needs limited assurance; an assurer asks *how each number was produced* and *who could change it*. We already held every primitive — this assembles them. `services/governance/assurance_pack.py` → `GET /v1/supply/report-snapshots/{id}/assurance-pack` returns a **ZIP**: `manifest.json` (every artifact **SHA-256-hashed**, tamper-evident), `methodology.md` (the H3→satellite→calibrated-€ pipeline, the **r²≥0.40 honesty gate**, and the 4-eyes/audit/immutable-snapshot controls), the frozen `report.json`, `validation_record.json` (backtests incl. the honestly-retired price claim), `audit_trail.json`, `approvals_4eyes.json`, `provenance.json`. Download button on each frozen version in the ESRS **Filed versions** panel; the export is itself audited. Verified on Terra Foods (7-file bundle, ~11 KB, manifest hashes present). tsc + web build pass. Runbook §4 → READY. |
| 2026-07-30 | 1.46 | **Point-1 workstream A — real Inline XBRL (iXBRL/ESEF) + a drop-in taxonomy binding + a validator.** Moves the XBRL gap from "tagged-data layer only" to "ESEF-shaped filing, one config file from the adopted taxonomy." **(1) Taxonomy binding** (`services/intelligence/esrs_taxonomy.py`) — a `TaxonomyProfile` separates the *tagging mechanism* from the *binding*: `provisional` (our `tesrs:` namespace, fully working, honestly labelled NOT a validated ESEF filing) vs `efrag_set1` (the real target). The adopted-taxonomy element map loads from `config/efrag_esrs_binding.json` the instant it's dropped in — **data, not code**; until then the profile honestly reports `pending_adopted_taxonomy` and `bound=false` per concept (proven: a partial map bound 3/12 to official `esrs:` names, the rest fell back flagged). A 12-concept catalogue carries each datapoint's DR, item-type and period-type. **(2) Inline XBRL** (`build_ixbrl`) — a human-readable + machine-parsable XHTML report with `ix:nonFraction` tags and proper instant/duration contexts (the ESEF shape), alongside the upgraded standalone `build_xbrl_instance` (both profile-aware). **(3) Validator** (`validate_document`) — structural + completeness checks (well-formed, contexts/units/decimals present & resolvable, concepts in catalogue) returning a per-check list; auto-runs **Arelle** if ever installed (absent here — disclosed, full conformance runs in the filing tool). Endpoints `/esrs-pack.ixbrl`, `/esrs-pack.validate`, `/taxonomy-binding`, + `profile=` on `.xbrl`/`.facts`; **Filing readiness** card on the ESRS page (iXBRL download + the check list + binding status). Verified on Terra Foods: iXBRL 12 inline facts, structurally valid, 2 contexts/3 units, provisional 12/12 bound. tsc + web build pass. Ops-readiness runbook §2 updated. |
| 2026-07-30 | 1.45 | **Ops-hardening: configurable reporting basis + immutable report snapshots + ops-readiness runbook.** Closed the three remaining gaps between "the numbers are right" and "a real assured filing goes out." **(1) Configurable reporting basis** — the ESRS/Taxonomy assemblers no longer hardcode scenario/horizon/materiality/period. Migration `org_reporting_settings_20260730` + `services/governance/reporting_settings.py` (`get_settings` = platform defaults merged over an optional per-org row); materiality threaded through `csrd_e1.build_e1_report(material_threshold=)`, `esrs_nature.build_esrs_pack(material=)`, `taxonomy_adaptation`, `esrs_xbrl`; supply.py `_reporting_basis()` helper resolves every filing endpoint (explicit `?scenario/?horizon` wins, else settings). `GET/PATCH /v1/admin/reporting-settings` (audited) + a **Reporting basis** editor on the Control-center Overview. **The r²≥0.40 publish gate is deliberately NOT settable — an honesty/scientific constant, not an org knob**, and the UI says so. Verified E2E: threshold 40→60 recomputed E3 live (8→1 water-stressed plots), reset clean. **(2) Immutable report snapshots** (`report_snapshots_20260730`, `services/governance/report_snapshots.py`) — freeze a filing (`csrd_e1` | `esrs_pack`) and its exact figures + reporting basis are captured **append-only, versioned per (org, report_type)**; a correction is a new version, nothing is ever overwritten. `POST /v1/supply/report-snapshots` (`reports.publish`, audited), `GET` list + `GET /{id}` full payload (`reports.view`); **Filed versions** panel on the ESRS page (freeze button + expandable version rows showing the frozen basis + key figures). **Immutability proven**: froze v1 at basis-40 (E3=8 plots), raised live basis to 60 (live E3→1), frozen v1 re-fetched still 8 at basis-40. **(3) Ops-readiness runbook** `docs/AGRI_OPS_READINESS.md` — honest READY / GAP(us) / GAP(customer) map of the last-mile plumbing to a regulator's inbox: EUDR Tier-2 TRACES API submit, XBRL binding to the adopted EFRAG taxonomy + iXBRL/ESEF, production geocoder (SLA + caching + address QA), the assurance evidence-pack export, and per-feed golden-source refresh cadence — each with an owner, none a rebuild. Migrations applied; import + tsc + live browser checks pass. |
| 2026-07-30 | 1.44 | **ESRS pack made filing-grade: XBRL tagging + EU Taxonomy climate-adaptation.** Closed the two gaps flagged in v1.43, honesty-first. **(1) XBRL tagging** (`services/intelligence/esrs_xbrl.py`): turns the Climate & Nature pack into ESRS **facts** — each carrying its disclosure-requirement reference (E1-9, E3-4/5, E4-5), entity+period+scenario context, unit, value — and emits a **well-formed XBRL instance** (validated via minidom). `GET /v1/supply/esrs-pack.facts` (JSON) + `/esrs-pack.xbrl` (XML); Excel + XBRL buttons on the ESRS page. Honest about the one dependency: concepts use a PROVISIONAL `tesrs:` namespace that must be bound to the adopted EFRAG ESRS Set 1 taxonomy in the filing tool — a real tagged-data layer + real XBRL instance shape, NOT a validated ESEF filing, and the file + UI say so. **(2) EU Taxonomy climate-adaptation** (`services/intelligence/taxonomy_adaptation.py`, `GET /v1/supply/taxonomy-adaptation`): the adaptation objective turns on a mandated **Climate Risk & Vulnerability Assessment** + evidence adaptation solutions address the material physical risks — exactly our own-ops physical-risk + adaptation layer. Emits CRVA coverage, asset value assessed, share materially exposed, candidate adaptation-contributing value, and an explicit **we-provide / you-provide** split — we do NOT compute an aligned turnover/capex/opex % (needs DNSH + minimum safeguards + financial tagging from the customer's suite). Surfaced as a section on the ESRS page. Verified on Terra Foods (XBRL: 12 facts, period 2025-12-31; Taxonomy: CRVA 100%, €310m assessed, 38.7% exposed, €120m candidate). build + tsc pass. |
| 2026-07-30 | 1.43 | **ESRS Climate & Nature disclosure pack + regulatory-scope strategy.** Made the reporting scope explicit and built the part we can own completely. `docs/AGRI_REGULATORY_SCOPE.md`: the regulatory map for an EU food/agri customer within our remit, the **dividing line = the data engine** (satellite/hazard/geolocation/deforestation), and per-regime **own / integrate / decline** — OWN EUDR + ESRS E1 physical + E3 water + E4 deforestation + Taxonomy climate-adaptation; **INTEGRATE** (don't build) GHG accounting Scope 1/2/3 (commoditized — Watershed/Persefoni/…); **DECLINE** social/governance; with the **Omnibus** timing caveat (treat CSRD/CSDDD/EUDR deadlines + thresholds as configurable, not constants). Built the **Climate & Nature pack** (`services/intelligence/esrs_nature.py`): E1 climate-physical (reuses `csrd_e1`), **E3 water** (worst water_stress/soil_water per site & plot → assets/spend exposed at ≥40), **E4 biodiversity/deforestation** (from the EUDR determinations: covered plots, deforestation-free %, non-compliant, post-cutoff forest-loss ha — only plots we actually determined count as free, never assumed), plus an explicit **out-of-scope** list rendered to the user ("handled by your carbon/EHS/HR/governance tool"). `GET /v1/supply/esrs-pack` (JSON) + `.xlsx`; `web/src/pages/EsrsPack.tsx` under Compliance → **"ESRS Climate & Nature"** (three topic cards with material flags + basis + drill-in to the E1 report and EUDR/DDS, the out-of-scope boundary, and basis-of-preparation). The point: deliver a **filing-grade importable component** that slots into the customer's wider CSRD statement rather than trying to be the whole suite. Verified live on Terra Foods (E1 €120m at risk; E3 8/20 plots water-stressed, €67.3m spend exposed; E4 2 EUDR plots 100% deforestation-free). Also fixed the async-scoring hang (add/edit/upload return instantly via a background daemon thread — fresh-cell add ~15s → 0.013s) and made Early-warning alerts click through to the commodity detail; global Back button. build + tsc pass. |
| 2026-07-30 | 1.42 | **Consolidated entry points — one governance door (customer) + "View as tenant" (us).** Design principle made explicit: **two front doors by login, each consolidated — never one merged screen** (a customer must never see cross-tenant data; that boundary stays a 403). **(A) Customer:** the **Control center** is now the single Governance nav link — **Approvals** and **Audit** fold in as **tabs** alongside Overview / Users / Roles / Approval matrix (`Approvals.tsx` & `Audit.tsx` gained an `embedded` prop that drops their standalone page header; the Overview summary cards switch tabs instead of navigating away). Tabs are **permission-gated**, so an *approver* (only `approvals.view`) opens the Control center to just the Approvals tab. **Product modules stay in the main nav** (daily-work surfaces, not admin) — deliberately not hidden under a hub. `Shell` `Item` gained `anyPerm` (show if the user holds ANY of a set). **(B) Us:** **"View as tenant"** audited impersonation — `POST /v1/ops/impersonate {org_id}` (platform.admin only) mints a session token as an active admin of the target tenant and records `impersonation.start` in that tenant's OWN `access_audit_log` against the real operator (transparency). **No change to the core auth path** — `get_current_user` already derives context from the token's `sub`, so the minted token "just works" as that tenant. Frontend: `auth.tsx` gained `viewAsTenant()`/`exitViewing()` (stash operator token → swap in tenant token → full reload); the Platform tenant drawer has a **"View as this tenant"** button; `Shell` shows a persistent **amber banner** ("Viewing X as a platform operator … recorded in their audit log") with **Exit to platform**. So one operator login (`ops@tellumen.io`) reaches every tenant's full app + cockpit, then exits cleanly. **Verified live E2E** (operator → view-as Terra → full workspace + Control-center tabs → exit restores operator); security: customer → `/v1/ops/impersonate` = **403**, entry lands in the tenant audit log. build + tsc pass. |
| 2026-07-30 | 1.41 | **Control center — a customer-admin cockpit + a platform-operator cross-tenant console (two personas).** Distinct from the operator's Home ("what's my risk"); these answer "is my org set up right" and "how are all tenants doing". **(A) Customer-admin Control center** (their own org): `GET /v1/admin/control-center` computes a **reporting-readiness checklist** (identity complete = EORI + filing contact; all sites scored; every >4 ha plot has a polygon; EUDR determination run; a second approver exists) + data-health counts + governance/access summary + entitlements; `PATCH /v1/admin/organization` edits the reporting identity (LEI/EORI/filing contact/operator address), audited. `web/src/pages/Admin.tsx` became the **Control center** with a new **Overview** tab (readiness ring + checklist, data-health stat row, editable identity panel, click-through cards into Approvals/Audit/Users) ahead of the existing Users/Roles/Approval-matrix tabs; nav renamed accordingly. Verified live on Terra Foods: **80% readiness (4/5)** with the one honest gap surfaced ("set EORI + filing contact"). **(B) Platform-operator console** (Tellumen staff, CROSS-TENANT): new permission **`platform.admin`** that NO customer role holds — the app is otherwise strictly org-scoped, so this is the one deliberate cross-tenant surface. `api/routers/ops_console.py` (prefix `/v1/ops`, separate from the pre-existing `/v1/platform` map/model router): `GET /ops/tenants` (all orgs + rollups: users, sites, plots, pending approvals, audit-30d, last activity, entitlements) + `GET /ops/tenant/{id}` (identity, users+roles, recent activity). Read-only. `web/src/pages/Platform.tsx` = tenant table + per-tenant detail drawer. Migration `platform_admin_perm_20260730` adds the permission; `scripts/seed_platform_operator.py` creates the **Tellumen (platform)** org + `platform-operator` role + **ops@tellumen.io / Demo!ops1**. **Persona separation**: the agri workspace nav is gated by `modules.view` (the operator lacks it → sees only the Platform group), and a platform-only user lands on `/platform`, not the empty agri Home. **Security verified**: ops sees all 6 tenants; customer admin@terra → `/v1/ops/tenants` = **HTTP 403**. `npm run build` + `tsc` pass; both personas walked live in the browser. |
| 2026-07-30 | 1.40 | **Sourcing self-upload + location governance (edit/delete under 4-eyes, full audit, admin console in the agri app).** Two things: (A) **Self-serve sourcing** — `POST /v1/supply/plots` adds one plot (geocode-or-latlon → H3 → score, audited); `GET /v1/supply/commodities` feeds a picker; `web/src/pages/Sourcing.tsx` gains a single-plot add form (shared `components/AddressAutocomplete.tsx` + commodity select) **and** bulk CSV upload/template with rich result feedback (unknown-commodity / bad-geometry / >4 ha-needs-polygon); `Operations.tsx` refactored onto the same autocomplete. (B) **Governance over every location change**, built on the platform's EXISTING RBAC + `approval_requests` (4-eyes, DB `checker≠maker` constraint) + `access_audit_log` engine — not a parallel one. New **approval matrix** `approval_policy` (migration `governance_locations_20260729`): per-org, per-action rules with `material_fields`, seeded platform defaults (site/plot × update/delete), admin-editable. New permissions `supply.locations.write` (maker) + `admin.approval_policy.manage`. `services/governance/location_governance.py`: `needs_approval()` reads the matrix (delete → always; update → only if a MATERIAL field changed — coordinates/value/spend), `submit_or_apply()` either applies directly (audited) or opens an approval request, and `apply_location_change()` is the ONE apply path (whitelisted columns, re-snap H3 + re-score on coordinate moves) used both for direct edits and on approval. `PATCH`/`DELETE /v1/supply/{site,plot}/{id}` route through it; `approvals.decide()` dispatches approved `supply.*` requests to the apply path. **Admin surfaces in web/**: Approvals inbox (approve/reject, own-request blocked), Audit trail viewer (`/v1/admin/audit`), Admin console (Users create/enable/role-assign, Roles permission editor, Approval-matrix toggles via `GET/PATCH /v1/admin/approval-policy`); a permission-gated **Governance** nav group; `DetailView` gains Edit/Delete (`components/LocationEditor.tsx`) with **diff-based PATCH** (a rename never trips the material-edit rule), material fields flagged "needs approval", and pending-vs-applied feedback. `api.ts` +`patch`/`del`. Demo seed adds **approver@terra.demo** (`Demo!approve1`) so the full maker→checker approve is demonstrable. Verified live end-to-end (analyst edits asset value → PENDING in the inbox; admin/approver clears → applied + audited; non-material edits apply directly). `npm run build` + `tsc` pass. |
| 2026-07-29 | 1.39 | **Base coverage for a food/agri company — own operations, business-interruption, a reusable detail workspace, and the CSRD/ESRS E1 report.** So a prospect sees a complete product, not pointers. **(1) Own operational sites** (`services/intelligence/company_sites.py`, migration `sc_company_sites_20260729` + `v_sc_site_physical_risk` view): a company adds its HQ/plants/warehouses (geocode-or-latlon → H3 → scored on the golden source) with **asset value (PP&E + inventory)** and **annual throughput**; `SiteLocationError` never fabricates a location. **Operations** page (`web/src/pages/Operations.tsx`): add-form + CSV upload/template, stat row (sites, elevated, asset value, BI exposure), table with Asset value / Throughput / BI columns. **(2) Business-interruption v0** (`bi_downtime_fraction(score)` → expected-downtime fraction by hazard band; BI = throughput × fraction; migration `sc_site_throughput_20260729`) — labelled illustrative, never dressed as validated. **(3) Adaptation layer** (`services/intelligence/adaptation.py`) — reference-backed "so what do I do" measures per hazard, surfaced on every detail view. **(4) Reusable detail workspace** (`web/src/pages/DetailView.tsx` for site/plot, `CommodityDetail.tsx` for a commodity) with a **static-tile `<img>` MiniMap** (`components/MiniMap.tsx`, replaces the maplibre paint-on-load quirk — always renders) + hazard bars + adaptation + CSV export; KPI tiles and biggest-exposures now click through to it. **(5) CSRD / ESRS E1 physical-risk report** (`services/intelligence/csrd_e1.py`, `GET /v1/supply/csrd-e1` + `.xlsx`, `web/src/pages/Csrd.tsx` under Compliance): the E1-9 anticipated-financial-effects section assembled from **own sites + sourcing book** — material hazards split **acute/chronic** and own-ops vs upstream, financial effects (**asset value at risk, BI, COGS-at-risk published vs exposure-mapped-but-withheld** — the same honesty gate, r²≥0.40), a forward hazard trajectory (current/2030/2050/2100), adaptation per hazard, and a provenance/basis-of-preparation block. Verified live on Terra Foods :5180 (€120m asset-at-risk, €4.6m COGS published vs €85.8m mapped-and-withheld across 7 commodities). Also persisted honest **held-crop calibrations** (Almonds PT drought r²=0.075, Sugar beet ES r²=0.177 — both below the 0.40 bar), so those crops now read "drought tested: explains X%, below our 40% bar" instead of "missing input". `npm run build` + `tsc --noEmit` pass. |
| 2026-07-29 | 1.38 | **Risk map reborn as the H3 hex grid — the "Earth broken into cells" data model made visible.** The agri Risk map (`web/src/pages/RiskMap.tsx`) is rebuilt on a single calm **light-blue "clear sky"** basemap (CARTO Voyager, full-opacity + contrast-boosted for clear land↔sea reading) and now draws the platform's actual index: **H3 hexagonal cells** around the sourcing book. New endpoint `GET /v1/supply/hex-hazard?res=N` (`api/routers/supply.py`): each plot's res-8 golden-source cell gets a **REAL per-cell hazard lookup** (`_compute_overall` over `canonical_scores`, heat_acute excluded so it's the standing climatology, not today's temperature), rolled up to the display hex (parent at the zoom-driven resolution) as the region's worst scored cell; a 1-ring of context cells shows the grid extending (status `no_data`). **Cells are clipped to country land** — new `services/reference/country_boundaries.py` intersects every hex with Natural Earth 1:50m admin-0 boundaries (`data/reference/ne_50m_admin_0_countries.geojson`) via shapely, so hexes follow coastlines/borders and offshore cells are dropped; the endpoint returns clipped `rings` (+ the plots/commodities in each cell). UI: cells rendered as an **SVG overlay projected via `map.project()`** (maplibre-gl 6.0.0 leaves vector/geojson layers blank until first gesture — SVG, like the DOM plot/label markers, paints on load); **hover a plot → detail tooltip, click a cell → one React info panel** (score + band + driver + plots list + H3 id, replaces on next click, closes on empty-map click — no more stacking popups). Also fixed the Vite dev worker drop (`optimizeDeps.exclude:['maplibre-gl']`). `npm run build` passes; verified live on :5180. |
| 2026-07-27 | 1.37 | **Greenfield agriculture UI (reinvention) — foundation + flagship EUDR Disclosure.** New app at `web/` (separate from legacy `ui/`), same FastAPI (no backend change) via Vite proxy → :8001. Stack: Vite + React 19 + TypeScript + Tailwind v4 (`@theme` tokens) + React Router + TanStack Query + **@xyflow/react** (React Flow) + Recharts + Framer Motion; Tellumen navy/sky design system, Fraunces/DM Sans/JetBrains Mono. Chose Vite-SPA over Next.js deliberately (JWT-in-browser auth; SSR/RSC buys nothing). Auth via `/v1/auth/login` + `/me`. Built + verified live: design system + `Shell` (sharp tabs) + the **Disclosure & EUDR flagship** — volume-at-risk stat row + bar chart, **Run EUDR check** (→ `/supply/eudr/determine`), a per-plot **declared-vs-our-satellite-determination** table, **Assemble DDS** (→ ready-to-file + blockers + operator-completes), and a **top-down / bottom-up lineage** (React Flow: satellite → filing, toggle to trace back). `npm run build` passes. **Full agri sector now built (all 7 tabs live against the API):** Disclosure & EUDR (determination + DDS + lineage), COGS-at-risk (per-commodity honesty tiers), Sourcing book (geolocated plot table), Risk map (react-leaflet dark map, plots coloured by live hazard), Early warning (/signals), Models & validation (the r² credibility record: 20 tested / 9 published / 11 held), Data foundation (sector-scoped, live agri golden-source count). (Backend suite unchanged at 329.) |
| 2026-07-27 | 1.36 | **EUDR/CSRD build — Phase 2 Tier 1: Due Diligence Statement assembler + reference capture (build it ourselves).** Closes most of the DDS gap in software — no TRACES credentials needed. `services/intelligence/eudr_dds.py` `assemble_dds()` builds a submission-ready DDS from what we already hold: operator identity, the EUDR-covered commodities (HS code + country of production), the geolocation of every plot, the standard due-diligence declaration (Art. 4/8), and OUR `deforestation_free` determination + evidence per plot. **Honesty gate**: a plot enters the fileable statement ONLY if determined `deforestation_free` with sufficient geolocation; non_compliant / geolocation_incomplete / insufficient / not-determined plots are explicit **blockers** (listed, never quietly filed), and fields the operator still completes in TRACES (net-mass quantity, EORI/address if missing, the signature) are surfaced as `operator_completes` rather than faked. Migration `eudr_dds_20260727`: operator identity on `organizations` (+eori, +operator_address) and an immutable `sc_eudr_dds` filing table (frozen payload + blockers + the TRACES reference/verification numbers). Endpoints: `POST /v1/supply/eudr/dds` (assemble + persist a draft/ready record), `GET /v1/supply/eudr/dds/{id}`, `PUT /v1/supply/eudr/dds/{id}/reference` (capture the reference the operator gets from TRACES → status 'filed'; refuses a draft that still has blockers). Verified live: Terra book → DDS with 2 cocoa plots (HS 1801, CI+GH), 0 blockers, honestly "not ready — operator identity incomplete", flipping to **ready to file** once EORI/address were set. **Tier 2 (direct TRACES API auto-submission as authorised representative) remains — it needs TRACES registration, an operational/legal step, not code.** `test_eudr_dds.py` +5 (ready/blocker/operator-gating). Suite 329 passed. |
| 2026-07-27 | 1.35 | **EUDR/CSRD build — Phase 1: deforestation-free determination engine (computed, replaces the declared flag).** The satellite answer, per plot. (1) Forest layer upgraded from raw loss to a real DEFORESTATION signal: `forest_loss_since` now masks loss by `treecover2000 >= 10%` (FAO-aligned "was it forest in 2000"), so pruning an olive grove or harvesting a never-forest plantation is NOT flagged — reads the Hansen treecover2000 band alongside lossyear via /vsicurl. (2) `services/intelligence/eudr.py` — `determine_plot(...)` combines the three facts EUDR turns on into one honest status: **not_covered** (commodity outside EUDR scope — no forest read) / **deforestation_free** (geolocated, no post-cutoff forest loss) / **non_compliant** (post-cutoff forest loss inside the plot, with the year) / **geolocation_incomplete** (a >4 ha plot given as a point) / **insufficient** (forest layer unreadable — never a green we didn't earn). Pure/testable; polygon boundary preferred, else the point. (3) Migration `eudr_determination_20260727` adds computed columns (eudr_determination/loss_ha/first_loss_year/forest_source/determined_at/evidence jsonb) — the customer's self-declared `eudr_status` is KEPT separately, so the product shows "you declared X; our satellite check says Y". (4) `POST /v1/supply/eudr/determine` runs the determination across a book (only covered plots hit the forest layer) and persists it, audited + idempotent; `/disclosure` now surfaces declared-vs-determination per plot + a computed summary (deforestation_free / non_compliant / geolocation_incomplete / insufficient). **Verified live end-to-end**: the real Terra book (20 plots) → 18 not_covered + 2 cocoa plots deforestation_free from a real Hansen read, 5 s. `test_eudr.py` +7 (mocked forest: coverage/geolocation/loss/insufficient paths), `test_forest.py` +1 (loss on non-forest masked out). Suite 324 passed. Roadmap next: Phase 2 (Art. 10 risk + DDS/TRACES). |
| 2026-07-27 | 1.34 | **EUDR/CSRD build — Phase 0b: forest-loss layer (Hansen GFC, read per plot).** The deforestation truth EUDR needs. `services/intelligence/forest.py` reads the **Hansen Global Forest Change `lossyear` raster** (University of Maryland, pinned `GFC-2024-v1.12`) and returns, for one plot geometry, the post-cutoff tree-cover loss inside it — `forest_loss_since(geom, cutoff_year=2020)` → has_loss / loss_pixels / loss_ha / first_loss_year / evidence. Loss counts only where lossyear ≥ 21 (2021+), i.e. **after the 31-Dec-2020 EUDR cutoff**; a point plot is buffered to a small disc to sample real pixels. **No bulk download** — GDAL range-reads the ~120 MB tiles over HTTP via `/vsicurl/`, windowing only the pixels under the plot (the same "golden source stays remote, compute per asset" pattern as ERA5); a tile can be pre-staged to `data/forest/` (`scripts/ingest_forest_baseline.py`, with a provenance manifest) for offline/fast reads and is used automatically when present. A read failure or off-grid plot returns `insufficient=True` — **never a fabricated "no loss."** Verified live: read the real 10N_010W tile remotely in ~2.7 s (no download) for a Ghana cocoa plot. **v0 honesty caveat**: the signal is tree-cover LOSS; it does not yet mask by `treecover2000` (was-it-forest) or separate deforestation from plantation harvest — so the output is reviewable EVIDENCE, not a final legal verdict (Phase 1 applies the EUDR rule on top). `test_forest.py` +5 on a synthetic tile (post-cutoff loss detected, pre-cutoff 2019 loss ignored, point buffered, missing-tile→insufficient). rasterio reads via `data/forest/` (gitignored). Suite 316 passed. |
| 2026-07-27 | 1.33 | **EUDR/CSRD build — Phase 0a: plot geometry foundation (no PostGIS).** First slice of the "close the reporting last mile" build. A sourcing plot was a POINT + optional area; EUDR needs the actual BOUNDARY (polygon mandatory >4 ha, point only ≤4 ha). Added `sc_sourcing_plots.plot_geometry` (jsonb GeoJSON, migration `plot_geometry_20260727`) and `services/intelligence/geometry.py` — GeoJSON parse (Point/Polygon/MultiPolygon, unwraps Feature/FeatureCollection), **geodesic area in hectares** (pyproj Geod on WGS84, not planar degrees²), centroid→(lat,lon) H3 key, and `validate_plot_geometry` enforcing the EUDR 4-ha rule honestly (a >4 ha point comes back `needs_polygon=True`, not silently accepted; a self-intersecting ring is rejected). **Deliberately no PostGIS** — the target Postgres has no postgis extension, so geometry is stored as GeoJSON and computed in Python (shapely/pyproj), portable + unit-testable; a PostGIS column can be added later as pure query-optimisation. The plot upload template + parser now accept an optional `plot_geojson` column (lat/lon become optional, derived from the polygon centroid; area auto-computed); bad geometry is reported per-row (`geometry_errors`) and >4 ha points are flagged (`needs_polygon`) rather than blocked. shapely/pyproj added to core deps. `test_plot_geometry.py` +8 (geodesic area, 4-ha rule, bad-JSON/bowtie rejection, Feature unwrap); DB round-trip verified. This geospatial upgrade is cross-sector (polygon-accurate scoring for banking collateral / real-estate parcels / insurance footprints), not agri-only. Suite 311 passed. |
| 2026-07-27 | 1.32 | **Multi-event / multi-driver crop backtest (fork 2) — verdict: heat is maize's true driver, but even so national maize stays honestly HELD; HEAT wired as a first-class crop driver.** Asked whether an event-level backtest could publish the maize origins the ranged tier held (US/ZA), WITHOUT the single-event circularity (`crop_fit.py`'s "cocoa trap"). Two rigorous findings: **(1) No transferable cross-origin drought law.** Treating each severe grain-fill drought (US 2012, ZA 1992/2016, AR 2009/2018) as one independent observation and doing **leave-one-EVENT-out** CV on a pooled loss~SPEI relationship gave **LOO r² = −0.71** (worse than the mean) — a unit of SPEI means completely different things in Iowa vs the Highveld vs the Pampas (ZA's *mildest* drought caused its *worst* loss). Pooling fails; there is no universal maize drought-sensitivity. **(2) US maize is HEAT-driven, not drought-driven** (Schlenker–Roberts killing-degree-days): loss~SPEI r²≈0.00 but loss~HEAT r²=0.35; the 1993 −19% loss was the Great Flood (a wet year — a third hazard neither index sees). So the honest fix was NOT a bespoke event tier but **completing the ranged tier's driver set**: added `heat_anomaly_score(z)=Φ(z)×100` to `heat_climatology.py` (the exact mirror of `drought_score`'s Φ(−SPEI)×100) and a `heat` driver to `fit_ranged_crop.py` — a per-year grain-fill temperature-anomaly percentile flowing through the SAME honest LOO/floor/band machinery as drought (no bolt-on). Re-fit maize on its correct driver: **US heat r²=0.28 (LOO 0.18), ZA heat r²=0.37 (LOO 0.25 — closest to publishable), AR drought r²=0.25**; the 2-driver SPEI+HEAT model was rejected — in-sample 0.44 collapsed to **LOO 0.11** (textbook overfit on n≈31). **Every maize origin still HELD below the 0.40 out-of-sample floor** — even on the correct driver + the event instrument, national maize yield is too buffered by irrigation/management and too dominated by non-climate variance to publish; forcing it would break the very floor that makes the book credible. Outcome: no new published crop (the honest answer to fork 2), but a real durable capability (HEAT as a crop driver) + a far more informative held record (US/ZA maize now correctly recorded as heat-driven with their LOO r²), reusable for future heat-limited crops and consistent with the projections' existing heat physics. `test_warming_amplification.py` +1 (heat_anomaly_score mirrors drought_score). Full suite 303 passed. |
| 2026-07-27 | 1.31 | **Global diversity / EUDR crop session — the discrimination story proven at world scale (8 tested, 0 forced, all honestly held).** Deliberately stress-tested the calibration method OUTSIDE the Mediterranean rain-fed cereal/olive belt where it publishes. Added 4 staple commodities (Maize, **Soybean — correctly flagged `eudr_covered`**, Sunflower, Rice) + 3 belts (`us_cornbelt` Iowa/Illinois/Indiana, `india_rice` Indo-Gangetic, `south_africa_maize` Highveld); ingested FAOSTAT yield for all; fit each on its own agronomic grain-fill window (drought SPEI-6, S-hemisphere summer crops on Jan–Mar so the season aligns to the harvest year). **Every one held below the 0.40 publish floor, each for a DISTINCT and real reason** — the method discriminates, it does not rubber-stamp: **US maize 0.01 / soy 0.01** (the Corn Belt had exactly ONE SPEI drought in 30 yrs — 2012, a clean −18% loss — and you cannot fit a dose-response *line* from a single dose; this is the ranged tier's structural limit, not a bug); **Argentina maize 0.25 / soy 0.13 / sunflower 0.04** (ENSO is two-sided — drought AND flood years — plus heavy tech-trend, and Argentine sunflower migrated north out of the belt); **India rice 0.13** (Punjab canal+tubewell irrigation buffers the monsoon — the textbook "irrigation decouples yield from climate" confirmation); **South Africa maize 0.20** (a REAL drought signal — large correct −0.39 slope, 2016 drought visibly cuts the crop — but too noisy to clear the bar, e.g. 2019's severe SPEI spared the main belt); **Brazil coffee 0.01** (frost/biennial-bearing driven, not dry-season SPEI — wrong instrument). All 8 persisted as `indicative`/held via `sc_commodity_fit`, so the product says "tested, climate explains X% of variance here, € withheld" with the r² shown — the same honest-hold treatment as the earlier TR/SY/AR/KZ wheat origins. **Key product finding**: the ranged-regression tier *structurally cannot* publish humid / irrigated / single-rare-event / wrong-driver crops; those would need the single-event backtest tier — which the project **deliberately refuses** for crops (`crop_fit.py`: the "cocoa trap", circular). So US-maize-2012 / ZA-2015-16 staying held is the honesty principle working as designed, not a gap. Net: the published book is unchanged (still the 9 Med rain-fed calibrations), but the TESTED book now spans the world's major staples incl. an EUDR commodity, materially strengthening the "we only publish where climate robustly drives yield" evidence base. No engine/UI change (holds surface automatically). 46 crop/region/drought tests pass. |
| 2026-07-25 | 1.30 | **Wildfire-region stress check — the instrument matters (a documented capability insight).** Prompted by the July-2026 Mediterranean fires (Bordeaux/Gironde vineyards, Sierra Norte de Guadalajara), added two fire-adjacent belts (`bordeaux_wine`, `spain_central` — NOT crop-calibrated; a stress-check) and ran our own drought/heat engine on ERA5 through June 2026 (`scripts/check_wildfire_stress.py`). Finding: the fires were **heat-driven flash desiccation, not accumulated drought**. HEAT (Apr–Jun anomaly) = the **hottest year in 36** for both belts (+3.7σ Bordeaux, +2.7σ central Spain) — our heat model flags it clearly. But our crop-yield default **SPEI-6 (Jan–Jun water year) reads "normal" (~32/100)** — the winter–spring was slightly wet, so the long agronomic window *misses the fire year*; a naive single-index read would falsely say "no drought stress". The short-window **SPEI-3 (Apr–Jun) catches it** (Bordeaux 81/100, 6th-driest of 36). Lesson kept: **acute / fire-adjacent risk needs heat + a SHORT-accumulation (flash-drought) index; the long SPEI-6 that drives our yield calibrations is the wrong tool for it.** This is also why a wildfire→crop € stays correctly **withheld** — wildfire isn't a validated crop driver, and even our drought number is mis-tuned for a heat-flash year. No engine change; a reproducible diagnostic + two reusable stress-check belts. |
| 2026-07-22 | 1.29 | **Projections v3 — 3-model CMIP6 ensemble (up from the 1-model ship).** The delta table now carries a **3-model coarse ensemble (MPI-ESM1-2-LR / IPSL-CM6A-LR / ACCESS-CM2)** with across-model spread; all covered belts (spain_olive + Maghreb/Iran wheat) re-scored on the ensemble mean. Finding: the ensemble did NOT moderate the single model — it **intensified** it, because the Mediterranean warming+drying is robust across models (Spain olive SSP5-8.5 2081-2100 ensemble **Δtas +5.18±0.88°C, Δpr −40.8±13.5%**, vs MPI-alone +3.9°C/−33%). Olive hot-house-2100 band €1.69–12.86m (1-model) → **€2.45–13.71m** (3-model); current book unchanged. The high-sensitivity anchor CanESM5 was intentionally dropped after it hung on GCS reads (same fine-grid stall as GFDL-ESM4/MRI-ESM2-0); the `n_models=3` column states this honestly, and the captured ±spread is ready to drive an uncertainty band. Suite 302 passed. |
| 2026-07-22 | 1.28 | **Projections v3 — RAW multi-model CMIP6 (delta-change), replacing the parametric warming/precip terms where covered.** The scenario × horizon axis now maps to a CMIP6 (SSP, period) and reads ACTUAL GCM output: orderly_1_5c→SSP1-2.6, disorderly_2c→SSP2-4.5, hot_house_3_5c→SSP5-8.5; 2030→2021-2040, 2050→2041-2060, 2100→2081-2100. `scripts/build_cmip6_deltas.py` pulls monthly tas+pr from the **Pangeo CMIP6 zarr archive** (Google Cloud, anonymous — reliable, unlike the CDS/ROOCS backend which failed) and computes the **delta-change** per crop belt: Δtas (°C) and Δpr (fractional) = future-period mean − the model's own 1995-2014 baseline (cos-lat area-weighted), so systematic model biases cancel. Ensemble mean + across-model spread → `data/cmip6/cmip6_deltas.csv`. `ml/scoring/cmip6.py` looks it up; the drought + soil_water scorers take optional `warming_c`/`precip_frac` overrides (CMIP6 when covered, else the v1/v2 parametric fallback for baseline/current and uncovered belts); `score_crop_drought` threads the per-belt delta. **Why it matters beyond v2**: the precipitation change is now DATA-DRIVEN per region with the correct sign everywhere — the Mediterranean dries (Spain olive SSP5-8.5 Δpr≈−33%, Morocco≈−48%) while **West Africa cocoa gets WETTER (+6%)**, which the parametric Med-box hack (drying-only, zero elsewhere) fundamentally could not represent. Effect: Spain olive hot-house-2100 band €0.55–11.61m → **€1.69–12.86m** (the floor lifts further — more Med loss near-certain under the real GCM drying); current book unchanged (baseline maps to no SSP). Shipped initially on **1 model (MPI-ESM1-2-LR)** with the 4-model coarse ensemble (MPI/IPSL/ACCESS/CanESM5, ECS ~3.0→5.6) building in the background; the CSV's `n_models` column states the ensemble size honestly, and the across-model spread is captured for a future uncertainty band. `test_cmip6.py` pins the mapping, the None-fallback, and (when the table is built) that a Med belt dries more under SSP5-8.5 than SSP1-2.6. Suite 302 passed. |
| 2026-07-20 | 1.27 | **Projections v2 — AR6 Mediterranean precipitation-decline term.** v1 dried a forward projection only through TEMPERATURE (more evapotranspiration). It missed the direct PRECIPITATION decline CMIP6 robustly projects for the Mediterranean — the defining regional "hotspot", and the one that matters most here because every crop we publish (olive, wheat, barley) sits in it. Added `precip_drying_spei(scenario, horizon, lat, lon)` in `heat_climatology.py`, grounded in **AR6 WGII CCP4: Mediterranean precipitation declines ~4% per 1 °C of GLOBAL warming** (high confidence above 2 °C), converted to SPEI z-units via a stated 1/CV mapping (Med seasonal precip CV ≈ 0.33). Folded into the drought + soil_water scorers as ADDITIONAL drying, applied ONLY inside the robust AR6 Med box (lat 28–47, lon −10–45) — 0 elsewhere, so we never invent a precip trend we can't cite (Iran's Zagros, lon >45, correctly gets none). Uses the GLOBAL delta (the AR6 %/°C is per global °C), not the latitude-amplified local warming. `score_crop_drought` threads cell lon; current horizon stays 0 (lane invariant preserved). Effect: Spain olive hot-house-2100 band €0–9.30m → **€0.55–11.61m** and durum wheat €0–7.76m → **€0.41–9.19m** — the band FLOOR lifts off zero, i.e. some Med loss becomes near-certain at hot-house-2100, the physical meaning of the hotspot; current book unchanged. Still PARAMETRIC (grounded in AR6's regional value), NOT a raw multi-model CMIP6 download+bias-correction — that (plus non-Med regional precip signals) remains the tracked next step. `test_warming_amplification.py` +4 (Med-only, current/coords zero, scales with warming, Med drought ≥ temperature-only). Suite 298 passed. |
| 2026-07-20 | 1.26 | **Second commodity validates — barley.** Tested the method on a fresh crop (barley, grown on drier/more marginal land than wheat in the same semi-arid belts). On the uniform rule (drought SPEI-6 Jan–Jun) barley PUBLISHES across the Maghreb: **Morocco B** (r²=0.58 in / 0.54 out), **Tunisia B** (0.51 / 0.43), **Algeria C** (0.54 / 0.45); Iran 0.38 (just under), Turkey/Syria/Spain held. Proves the calibration ladder isn't wheat-specific — it validates a second cereal cleanly where climate genuinely drives yield. Added Barley to the FAOSTAT ITEM_MAP + a Barley commodity (drought/soil_water, measured basis = grain); ingested barley yield for all 25 origins. Rebuild: `ingest_crop_yield_faostat --commodity Barley` → `fit_ranged_crop --commodity Barley --origin {MA,DZ,TN} --region {morocco,algeria,tunisia}_wheat --driver drought --spei-scale 6 --season 1,2,3,4,5,6`. Credibility book now spans 2 cereals + olive: **9 published crop×origin calibrations** (Wheat MA-A/TN-A/DZ-C/IR-C, Barley MA-B/TN-B/DZ-C, Olive B, Durum wheat C). Suite 294 passed. |
| 2026-07-20 | 1.25 | **Rain-fed book expansion + the method's discrimination shown honestly.** Tested five more rain-fed wheat origins on the uniform rule (drought SPEI-6 Jan–Jun water year, same as olive) — publishing only those that clear the 0.40 fit-floor, withholding the rest with a stated reason. **PUBLISHED: Iran** (western Zagros dryland, r²=0.42 in / 0.33 out → Confidence Grade C) joins Morocco (A), Tunisia (A), Algeria (C) — four grade-A/C wheat origins now on the credibility record. **HELD, each for a distinct real reason** (the honesty selling point — the method discriminates, it does not rubber-stamp): **Turkey** ~0.25 (Konya irrigated + national aggregate mixes irrigated/rain-fed → diluted, as Australia); **Syria** ~0.38 (genuinely drought-driven but the civil war contaminates the modern series — post-2011 collapse is displacement not weather, and the clean pre-war window is too short / single-point-leveraged); **Argentina** ~0.03 (humid temperate Pampas is not water-limited); **Kazakhstan** ~0.29 (short post-Soviet series with an early-1990s privatization structural break + vast-steppe aggregation). Held origins NOT persisted (none clears; Syria's number would mislead without the war caveat the UI can't show). Added 5 ERA5 regions (turkey/syria/argentina/iran/kazakhstan_wheat); ingested Kazakhstan wheat yield (FAO area code 108, was missing; n=33 1992–2024). No engine/UI change — Iran surfaces automatically on the Models & validation credibility record via the global ranged_fits query. Suite 294 passed. |
| 2026-07-19 | 1.24 | **Projections upgrade — regional warming amplification (AR6-grounded, parametric v1) + two more grade-A rain-fed crops.** (1) The scenario deltas (`SCENARIO_WARMING_C`) are GLOBAL-MEAN warming, but assets sit on LAND, which warms faster than the mean and more so toward the poles. `ml/scoring/heat_climatology.py` now centralises the shift in `warming_delta(scenario, horizon, lat)` = global delta × `warming_amplification(lat)` where `amp(lat)=LAND_BASE(1.40)+POLAR_K(1.5)·(|lat|/90)²`, capped 3.0 — reproducing AR6 zonal LAND ratios (equator ~1.40, Mediterranean 37° ~1.65, N-Europe 52° ~1.85, sub-Arctic 65° ~2.18). All five climatology scorers (heat/drought/soil_water/frost/heat_chronic) take an optional `lat` and route through it; the point-scorers and crop/cocoa belt-scorers thread the cell latitude. **The current horizon is 0 warming, so amplification NEVER touches a live score — only 2030/2050/2100 projections** (score-lane invariant preserved; re-scored belts retire only their own standing forward scores). Effect on the flagship: Spain olive hot-house-2100 band widens €0–7.4m → **€0–9.3m** (Mediterranean warms ~1.65× global); tropical cocoa barely moves (lat ~6 → ~1.44×, physically correct); Terra book current €4.65m UNCHANGED, hot-house-2100 total €10.4m → €13.3m. This is a PARAMETRIC shift of today's climatology, honest about land+latitude — NOT downscaled CMIP6 (still the tracked next step); it does not add the Mediterranean summer DRYING hotspot beyond temperature. `tests/unit/test_warming_amplification.py` pins the invariants (monotone in lat, symmetric, capped, current-horizon never amplified). (2) Fitted **Algeria + Tunisia wheat** (Maghreb rain-fed durum, drought SPEI-6 Jan–Jun water year, same rule as olive): **Tunisia r²=0.636 in / 0.575 out → Confidence Grade A** (2nd grade-A crop), **Algeria r²=0.565 / 0.479 → grade C**; Morocco stays A. Foundational fix: `sc_commodity_calibration.world_share` was stale-NOT NULL (blocked the fit INSERT path for a new origin with no curated share) → migration `world_share_nullable_20260719` drops it (read-path already sums only non-NULL shares). (3) The Models & validation credibility record now shows origins as readable country names (`originName` in the UI vocabulary; "Wheat · Morocco", not "Wheat MA"). Suite 294 passed. |
| 2026-07-19 | 1.23 | **Water-availability layer + Confidence Grade + rain-fed proof + the honest reservoir null-result.** (1) **Root-zone soil water** as a DISTINCT hazard `soil_water` (ERA5-Land layers 2+3, depth-weighted; `ml/features/soil_moisture.py`, `ml/scoring/soil_water_climatology.py`; added to `core.types` + hazard-vocab migration). Tested head-to-head vs SPEI per crop (`scripts/compare_water_drivers.py`): olive tie, wine SPEI wins, **dryland wheat soil-moisture WINS (0.36→0.445)** — so Spanish durum wheat now PUBLISHES ranged (r²=0.42, band, driver=soil_water). A cell carries BOTH hazards; a calibrated crop displays its own driver. (2) **Rain-fed origins prove the thesis**: ingested wheat yield for 7 rain-fed origins (MA/DZ/TN/AU/TR/SY/AR, 64yr FAOSTAT); **Morocco wheat = first grade-A crop** — drought SPEI-3 Jan–Apr, in-sample r²=0.673, **out-of-sample 0.629**, because a genuinely rain-fed crop is not decoupled from climate by irrigation. (3) **Confidence Grade A–E** (`ml/confidence_grade.py`): transparent 4-check composite (out-of-sample LOO-CV r² + evidence depth + band calibration + proof type), honesty cap (weak predictive → max C), sits ON TOP of the visible stats (deliberately NOT a rename of r²); shown via `ui/src/components/GradeBadge.jsx` on CogsCommand + SupplyModels. `crop_fit.py` now emits `r2_oos` + `band_cov68`; migration `fit_validation_stats_20260718`. (4) **Reservoir/irrigation water — tested, does NOT clear the bar, crops stay held (honest null-result).** Distilled the MITECO Boletín Hidrológico MDB (weekly storage 1988–2026, every Spanish reservoir >5hm³) into a compact per-basin monthly fill index (`scripts/build_reservoir_index.py` → `data/reservoirs/basin_reservoir_index.csv`; raw 213MB MDB gitignored). Regressed decomposed yield on basin fill (`scripts/compare_reservoir_driver.py`): beet/Duero r²=0.02 (wrong sign), citrus/Júcar+Segura r²=0.07 (right sign, LOO-negative), almonds r²≈0.00. **Interpretation: irrigation is a buffer — when it works it DECOUPLES yield from water availability, so basin storage only bites in rare hard-restriction years and can't reach r²=0.40 on ~18–34 noisy national points**; beet is further confounded by the 2006 EU sugar-quota area collapse (a policy signal). No number faked; these crops remain held. The reservoir index + harness are kept as durable assets for future restriction-year / projection work. |
| 2026-07-18 | 1.22 | **Agriculture crop calibration — the 'ranged' tier + honest cyclical-crop validation.** Three-tier calibration derived (never typed) in `v_sc_commodity_calibration`: **indicative** (v0 defaults, € withheld) → **ranged** (a driver explains the crop PARTLY; € published as a BAND with r² stated) → **backtested** (reproduces a real event; € as a point). `sc_commodity_fit` stores the multi-year OLS (slope/intercept/r²/rmse + n/score_mean/score_sxx for a real prediction interval); `ml/features/crop_fit.py` fits, `scripts/fit_ranged_crop.py` persists (MIN_R2=0.40). The engine (`services/intelligence/supply_cogs.py`) emits `volume_at_risk_low/high_eur` + `fit_r2` for a ranged origin, floored at 0 (a favourable year is a gain, not "volume at risk"); the publish gate lets 'ranged' through. **Cyclical-crop validation target** (`ml/features/world_shock.py`): a crop that alternate-bears (olive/wine/almonds) must NOT be validated against FAO's raw world shock — it bundles the tree cycle and nets damage against other origins' good years. Decomposed into raw / net / **damage** (losses only), the damage figure is the target a damage-only model can reproduce. Ingested 13 material FAOSTAT origins (Türkiye/Morocco/Syria/etc.; China=41 mainland not the 351 aggregate) lifting olive world coverage 66%→97%. **Reference crop: Spain olive**, drought SPEI-6 (the agronomic water-year window, r²=0.51 over 31 yrs; heat adds nothing), scored onto the belt (`scripts/score_crop_drought.py` (parametric: region+commodity), standing lane, scenario×horizon) → olive flips from "€ pending" to a live range that rises under warming (baseline €0–€4.1m → hot-house-2100 €0–€7.4m). **USP finding**: tested and DISPROVED that finer spatial resolution tightens the fit (r² 0.51 belt vs 0.47 Jaén) — the ceiling is national crop-data noise, not hazard resolution; so the pitch is footprint-breadth + filing-integration + auditability + honest-uncertainty (the band is a trust asset) + live/early, NOT point-accuracy. `measured_basis` disclosed per crop (olive = the fruit, not oil). Retired the dead price-amplification chain from the published path. Suite 278 passed. |
| 2026-07-13 | 1.21 | Enterprise completeness sweep (8 gaps closed to widen the beachhead). **FX** — a non-EUR line (market_value + currency) converts to EUR at the ECB reference rate on-or-before the book's as-of date; fx_rates table (seeded ECB 2023-12-29, scripts/load_fx_rates.py pulls live), fund_positions stores native value+currency; unknown currency is a surfaced error, never assumed. **Taxonomy DNSH gate** — issuer_esg_metrics gains dnsh_ok/min_safeguards_ok; an explicit FALSE excludes that issuer's reported aligned% (surfaced as aligned_excluded_dnsh_pct); NULL preserves prior behaviour. **Voluntary PAI** — ml/regulatory/voluntary_pai.py catalog of real RTS Table 2/3 indicators; per-fund selection (fund_voluntary_pai) + per-issuer values (issuer_voluntary_pai); computed roll-up (weighted-avg / share-of-value) replaces the declaration stub; GET /voluntary-pai/catalog, PUT /funds/{id}/voluntary-pai. **Entity-level roll-up** — entity_pai_statement value-weights every position across ALL a manager's funds (scope override on fund_pai/fund_esg_pai/_taxonomy_rollup/_composition/voluntary; shared _mandatory_indicator_rows); GET /entity/sfdr-statement + per-fund coverage table. **Batch orchestration** — sfdr_batch_runs/items + services/sfdr_batch.py generate statements across the whole book, resumable (chunked via limit, one bad fund can't abort); POST /entity/sfdr-batch(/{id}/run), GET /entity/sfdr-batch/{id}. **Vendor connector** — services/reference/vendor_ingest.py maps MSCI/ISS (or custom) extracts, matches by ISIN/LEI, stores source='vendor'; read-path precedence own>vendor>global (tiebreaker added to the emission/ESG/taxonomy LATERALs); POST /vendor/ingest, GET /vendor/profiles. **XBRL** — ml/regulatory/sfdr_xbrl.py emits a valid XBRL instance (contexts=manager LEI+period, units, tagged facts, PAI1 split by scope); GET /funds/{id}/sfdr-statement.xbrl + /entity/sfdr-statement.xbrl (Tellumen placeholder taxonomy, swap for ESMA's). **UI** — header-aware holdings paste exposes every intake column (currency, EVIC, ESG 5-14, Taxonomy+DNSH); XBRL download on the statement page. Also fixed 2 pre-existing test failures (frost hazard alias; packages error-envelope assertion). Full suite 218 passed. |
| 2026-07-04 | 1.0 | Initial Software Description Document created. |
| 2026-07-11 | 1.1 | Added asset-manager securities book + reference-data resolution layer (open-data ISIN→issuer→footprint via GLEIF + Nominatim, with provenance and an ISIN-resolution audit log). |
| 2026-07-11 | 1.2 | Added SFDR PAI statement (the filing): ml/regulatory/sfdr_pai.py assembles the mandated RTS Annex I Table 1 (14 investee indicators — computed where honest, gap-flagged with the exact input otherwise) + EU Taxonomy lines; JSON + downloadable .xlsx endpoints; asset-mgmt SFDR filing UI. Common EU-listed universe pre-loaded via scripts/load_reference_universe.py. |
| 2026-07-12 | 1.3 | Client issuer-data enrichment: onboarding accepts optional NACE/revenue/scope1-3 per holding to fill more of the SFDR statement. Emissions are org-scoped private disclosures (issuer_emissions.org_id; global org_id NULL = estimated/public fallback), source='client'; NACE stays a global fact (enrich-if-unknown). fund_disclosure prefers the org's own disclosure over the global fallback. |
| 2026-07-12 | 1.4 | Emissions estimation gap-fill: services/reference/emissions_estimation.py estimates scope 1+2 = NACE sector-average intensity × revenue when no scope is disclosed (source='estimated', method disclosed; scope 3 not estimated). Wired into onboarding. SFDR statement now discloses the reported-vs-estimated split (emissions_estimated_pct) per RTS. Intensity coefficients are illustrative sector averages pending an EXIOBASE-sourced table (flagged in code). |
| 2026-07-12 | 1.5 | Sovereign & real-estate PAI tables (RTS Annex I indicators 15–18). Sovereign 15 (GHG intensity of investee countries) computes from a public country-intensity table when the fund holds sovereign bonds; 16 (social violations) gap-flagged. Real-estate 17–18 shown as not-applicable for securities funds (applies to direct property). SFDR statement JSON/xlsx/UI gain sovereign_indicators, real_estate_indicators, holdings_composition. |
| 2026-07-12 | 1.6 | Universe pre-load scaling: geocoder is now config-driven (NOMINATIM_URL + NOMINATIM_MIN_INTERVAL_S) so a self-hosted Nominatim drops in without code changes; rate limiter is thread-safe; loader gains --workers for parallel resolve+locate (safe default 1 against public Nominatim's 1 req/s policy, raise only with a permissive/self-hosted geocoder). The remaining scale limit is deployment (run a self-hosted geocoder), not code. |
| 2026-07-12 | 1.7 | Financed emissions PAI 1/2 via EVIC. Migration c2d3e4f5a6b7 adds issuer_emissions.evic_eur. Onboarding accepts evic_eur per holding; fund_pai computes PCAF financed emissions (attribution = market value ÷ EVIC) and PAI 2 carbon footprint (financed ÷ €M invested), with financed_emissions_coverage_pct disclosed and computed/partial by coverage. SFDR statement/UI/xlsx show PAI 1 as financed. Fixed: enrichment ON CONFLICT now COALESCEs so a partial follow-up (e.g. EVIC only) fills gaps without erasing earlier figures. |
| 2026-07-12 | 1.20 | Article 8/9 periodic report (SFDR RTS Annex IV/V). ml/regulatory/sfdr_periodic.py assembles the periodic disclosure: product characteristics, E/S attainment (sustainability indicators computed, narrative flagged), EU-Taxonomy alignment (from the alignment roll-up), asset allocation + sustainable-investment share (need the manager's per-holding classification — flagged), PAI-considered, and top investments. GET /v1/funds/{id}/periodic-report; new "Periodic report" UI page + nav. Article-8/9 only. |
| 2026-07-12 | 1.19 | Look-through expansion. POST /v1/funds/{id}/look-through takes a held fund/ETF's constituents, creates a sub-fund (parent_fund_id) holding them (values scaled to preserve the wrapper's exposure), and removes the wrapper position — so the underlying issuers flow into the fund's PAI via the existing descendant roll-up, with no double-count. look_through status reports applicable/expanded honestly. |
| 2026-07-12 | 1.18 | EU Taxonomy alignment. Migration taxonomy_align_20260712 adds issuer_esg_metrics.taxonomy_eligible_pct / taxonomy_aligned_pct — the issuer's own Article-8 reported figures (org-scoped disclosure, supplied on onboarding). _taxonomy_rollup value-weights the reported aligned/eligible % over the whole book, discloses alignment_coverage_pct, and asserts alignment ONLY from reported issuer data (never infers DNSH/minimum-safeguards). UI taxonomy cards show real aligned/eligible % + reported-coverage. /health fixed (duplicate handler removed; real text()-wrapped DB probe). |
| 2026-07-12 | 1.17 | Correctness/honesty audit fixes (fresh-eyes review of the SFDR module). (1) EVIC attribution factor capped at 1.0 and EVIC/revenue required strictly positive (Holding gt=0, scopes ge=0) — a tiny/mis-keyed/negative EVIC no longer inflates or inverts financed emissions PAI 1/2. (2) Year-on-year change only computed when prior & current indicator share the same method — an un-attributed→financed PAI 1 no longer fabricates a ~99% move (shows prior value + a "not comparable" note instead). (3) Umbrella funds: reference-year, taxonomy and composition/sovereign now roll up fund descendants (were parent-only). (4) Reference-year detection org-scoped + counts distinct issuers (was raw rows across tenants). (5) Cross-tenant leak fixed: issuer_detail emissions read now org-scoped. (6) Negative revenue excluded from WACI. (7) Duplicate ISIN lots aggregated, not dropped (weights were understated). (8) PAI 4 fossil coverage now reflects known-NACE share (was hard-coded 100%). Regression tests added for the EVIC cap and YoY method-mismatch. |
| 2026-07-12 | 1.16 | Remaining filing pieces. (1) PCAF data-quality score (1 best–5 worst), value-weighted from the emissions source per holding, in fund_pai + statement + UI. (2) Additional (voluntary) PAI declaration block (RTS Tables 2&3 — manager must adopt ≥1 climate + ≥1 social; surfaced as declaration_required). (3) Look-through detection from asset_class (held funds/ETFs flagged for constituent expansion; n/a for direct securities). (4) Mandatory narrative sections (migration narratives_20260712 → organizations.sfdr_narratives JSONB): policies / actions / engagement / standards; set via /manager/filing-profile; now gate filing readiness; UI narrative form + xlsx summary rows. |
| 2026-07-12 | 1.15 | Prior-year comparison (SFDR year-2 requirement). Migration sfdr_filings_20260712 adds fund_sfdr_filings — an immutable frozen statement snapshot per fund per reference year (mirrors bank_disclosure_submissions). POST /v1/funds/{id}/sfdr-statement/file freezes the current statement; GET /sfdr-filings lists history. sfdr_pai looks up the most recent prior filing and attaches each indicator's prior_value + change/change_pct + a comparison block. UI: "File statement" button (enabled once ready to file), a "vs FY<prior>" badge, and per-indicator ▲/▼ change vs prior. Two-period demo (files FY2022, shows FY2023 YoY). |
| 2026-07-12 | 1.14 | Filing identity + readiness. Migration filing_identity_20260712 adds LEI/legal_name/filing_contact_email to organizations (the FMP) and lei to funds. PUT /v1/manager/filing-profile validates the manager LEI against GLEIF (real, active) and auto-pulls the legal name; PUT /v1/funds/{id}/lei for the fund LEI. The SFDR statement gains a filing_readiness block (ready_to_file + missing entity fields); UI shows a green "Ready to file" or an amber inline LEI/contact form that flips it. This is the gate between a computed statement and a submittable one. |
| 2026-07-12 | 1.13 | Non-carbon PAI indicators (5-14) made fillable. New issuer_esg_metrics table (org-scoped, migration esg_metrics_20260712) holds energy mix/intensity, biodiversity, water, hazardous waste, UNGC/OECD violation + monitoring flags, gender pay gap, board diversity, controversial weapons. Onboarding accepts them; services.fund_disclosure.fund_esg_pai computes PAI 5-14 (value-weighted ratios, exposure shares for flags, EVIC-attributed absolutes for water/waste); sfdr_pai fills those rows with coverage. A fully-documented book now computes all 14 mandatory indicators — 0 left "awaiting input". |
| 2026-07-12 | 1.12 | EXIOBASE calibration RUN. scripts/build_nace_intensities.py now computes real NACE sector GHG intensities from EXIOBASE 3 (IOT_2022_ixi, impacts/S GWP100 ÷ output, EU-region output-weighted), pulling only the two needed matrices (~23 MB) from the 755 MB archive via HTTP range requests (remotezip). data/reference/nace_emission_intensity.csv now holds EXIOBASE values for the 53 divisions EXIOBASE distinguishes (e.g. electricity 1247, air transport 735, software 14, banking 16.5 tCO2e/€M); the estimator merges these over the embedded interim table so the ~32 divisions EXIOBASE folds (e.g. pharma) keep coverage. MODEL_VERSION → emissions-est-v2-exiobase. Tests assert the EXIOBASE values load and pass sanity ranking. |
| 2026-07-12 | 1.11 | Coefficient calibration & externalisation. Intensity coefficients moved out of code into provenanced data files: data/reference/country_ghg_intensity.csv (REAL values computed from OWID / Global Carbon Project CO2 ÷ GDP, 2022, via scripts/build_country_intensities.py) and data/reference/nace_emission_intensity.csv (interim sector averages, source-flagged; scripts/build_nace_intensities.py documents the EXIOBASE 3 calibration pipeline). emissions_estimation + sfdr_pai load the CSVs at runtime (embedded fallback offline). Statement provenance cites the actual sources/vintages. Tests: intensities load from the data files and sovereign values are the real OWID-derived figures. |
| 2026-07-12 | 1.10 | Data-quality hardening: tests around the make-or-break math — emissions estimation (intensity×revenue, safe degradation, no scope-3 estimate), reference-resolution guard clauses (malformed ISIN/LEI rejected before any network call), PAI indicator structure (all 14 present, real-estate not-applicable, sovereign computes/gaps), and exact-arithmetic PCAF integration tests (financed emissions, WACI, carbon footprint; partial-without-EVIC). Estimation/country intensity coefficients remain flagged in-code as illustrative pending EXIOBASE calibration before a production filing. |
| 2026-07-12 | 1.9 | Design-partner self-serve: GET /v1/holdings/template.csv returns a documented holdings template (required isin+value; optional NACE/revenue/scope1-3/EVIC/asset_class/reporting_year) a manager fills with their own book; onboarding UI gains a "Download template" link. Trial path is the existing demo login. |
| 2026-07-12 | 1.8 | Filing-grade output. sfdr_pai_statement gains an RTS declaration summary (manager/LEI, reference period from emissions vintage, prior-period N/A, PAI-considered) and a provenance appendix (data sources + vintages, model versions, reported/estimated/financed coverage, methodology notes). Downloadable .xlsx is now a 3-sheet workbook — Summary, PAI statement (RTS Table 1 columns: Indicator/Metric/Impact[ref]/Impact[prior]/Explanation/Actions, grouped investee/sovereign/real-estate), Provenance & methodology — with fit-to-width print setup. Manager LEI surfaced as a required input (organizations has no LEI column yet). UI shows reference period + LEI-required + download contents. |
